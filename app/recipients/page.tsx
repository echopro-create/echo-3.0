export const metadata = { title: "Получатели — ECHO" };

export default function RecipientsPage(){
  return (
    <div className="wrap">
      <h1 className="title" style={{textAlign:'left'}}>Получатели</h1>
      <div className="card" style={{marginTop:12}}>Список адресатов (заглушка).</div>
      <div className="card" style={{marginTop:12}}>Форма добавления адресата (заглушка).</div>
    </div>
  );
}
